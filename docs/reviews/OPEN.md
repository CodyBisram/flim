# Open findings from the nightly review

One row per finding the nightly review has raised. The review re-verifies every row marked
`open` or `unverified` against HEAD each night and moves it to `fixed` (with the commit) or
`not a bug` (with one line why), so this file is the truth about what is still wrong, and
`docs/reviews/<date>.md` is the evidence. Seeded 2026-09-13 from thirteen review files that had
sat unread in pull requests; everything starts `unverified` and the first night triages it.

| Raised | Severity | Finding | Status | Note |
|---|---|---|---|---|
| 2026-08-27 | high | Flim/ContentView.swift:117 (account-change flush ran after the cache resets, dropping a pending undoable action) | fixed | 92c1fb9 |
| 2026-08-27 | high | Flim/Views/Rolls/RollsView.swift:397-398 ("Shoot into this roll" posts a bare NotificationCenter notification instead of CameraRollSelection.select, camera can point at the wrong roll) | fixed | e68e879 |
| 2026-08-27 | high | Flim/Views/Feed/FeedView.swift:555 (onAuthorBlocked), snapshotLedger at 881-904 (re-snapshots the ledger non-growOnly, can shrink the header's counts for unrelated already-read units) | fixed | d0b1129 changed onAuthorBlocked to call snapshotLedger(growOnly: true) |
| 2026-08-27 | high | Flim/Views/Profile/BadgePickerSheet.swift:670-685 (commit(), staging in save() at 651-668) (commit() bypasses UndoCenter, a stale staged clear-badges action can overwrite a fresh save) | open | |
| 2026-08-27 | medium | Flim/Views/Rolls/RollsView.swift frame-counts race (mine defaulted to 0 between two sequential writes) | fixed | 92c1fb9 |
| 2026-08-27 | medium | Flim/Views/Feed/UserPageView.swift:335-355 (blockAccount defers dismiss into the staged commit closure, can pop whatever the user navigated to since) | open | |
| 2026-08-27 | low | Flim/Views/Profile/ProfileView.swift:512-518 (photoError auto-dismiss timer is never cancelled, a second error can be cut short by the first one's timer) | fixed | d0b1129 added photoErrorDismiss task, cancelled before each new one starts |
| 2026-08-28 | high | Flim/Views/Rolls/RollDetailView.swift:617-637 (reveal re-presents on every reappearance; presenting never marks it seen, only genuine completion does) | fixed | 05cd8bd added server-side completed_at (complete_reveal_view) and RollRevealViewModel.dismissedThisLaunch, bounding re-presentation to once per app launch |
| 2026-08-28 | medium | RollRevealView.swift selection/index desync after skipDeadFrame | fixed | 92c1fb9 |
| 2026-08-28 | low | Flim/Views/Rolls/RollRevealView.swift:480-484 (stages a reportPhoto undo but has no undoCapsuleHost of its own, capsule unreachable during the reveal) | open | |
| 2026-08-28 | low | SharePreviewSheet.swift Share button not gated on render completion | fixed | 92c1fb9 |
| 2026-08-30 | low-medium | Flim/Views/Feed/FeedView.swift:311 (fetch), 777 (gate), reload() at 828-888 (inviteQuota fetched once in .task, never refreshed by reload(), can hand out an already-dead invite code) | open | |
| 2026-08-31 | medium | Flim/Services/InstantFilmProcessor.swift:677-678 (flashFalloff floors peak at 0.02 instead of treating it as 1, darkening an already near-black flash frame instead of leaving it untouched) | fixed | cb2529c added flashAnchorThreshold/flashAnchorFullStrength with flashFalloffStrength's smoothstep ramp; below threshold the frame returns untouched |
| 2026-09-01 | medium | `admin_overview()`'s never_asked_notifications (2026-08-31_admin_overview_v2.sql:88-93) adds a device_tokens condition admin_reach()'s never_asked lacks, so the two panels disagree | open | |
| 2026-09-01 | medium | `admin_overview()`'s reveals week-over-week (2026-08-31_admin_overview_v2.sql:31-32,57-61) compares an 8-day "now" window to a 7-day "prev" window | open | |
| 2026-09-03 | high | PhotoGridCell.swift disk-cache-miss path had no loadGeneration guard | fixed | 92c1fb9 |
| 2026-09-03 | low | Flim/Services/RollSnapshotStore.swift:55-61 (save() spawns an unordered detached Task per call; two persistSnapshot() calls for one account can finish out of order) | open | |
| 2026-09-05 | high | Flim/Views/Profile/ChapterRecapView.swift:387-395 (recap pager still resolves reactions/comments by photo id against photo_reactions/photo_comments, not the post_reactions/post_comments chapter_stats() counts from) | fixed | 05933d7 |
| 2026-09-05 | medium-high | Flim/Services/PhotoService.swift:448-465 (generic upload-failure catch never calls patchEarlierBurstGroup; the retry path at :393-396 also returns patchEarlier: nil, so the obligation is permanently dropped) | fixed | e68e879 moved patchEarlierBurstGroup to fire right after burst resolves, before the insert's do/catch, so it now runs regardless of what happens to this capture's own insert |
| 2026-09-05 | medium | Flim/Views/Rolls/RollDetailView.swift:921-937 (openAwaitingPhotoIfReady reads awaitingPhotoComments at line 931, after polling, instead of capturing it with photoId at entry; a second push for the same roll can attach the wrong comment intent) | open | |
| 2026-09-05 | ? | Burst grouping's 3-second window is measured against a timestamp taken after the previous shot's full pipeline | fixed | 39d08b8 |
| 2026-09-05 | medium | Flim/Views/Profile/ChapterRecapView.swift:472-474 ("Play again" sets isPlayerPresented = true without resetting selection to 0, so replaying after viewing a stat-line photo resumes from that photo's index instead of the start) | open | |
| 2026-09-05 | low | Flim/Views/Rolls/RollDetailView.swift:921-937 (openAwaitingPhotoIfReady only checks !showReveal in the entry guard at line 922, not again before the final selectedPhoto = photo assignment at line 936, so a reveal that starts mid-poll can still get a photo cover presented on top of it) | open | |
| 2026-09-05 | low | Flim/Services/BurstGrouping.swift:49 (sharpest(in:) coalesces nil sharpness to 0 via a.sharpness ?? 0, so a genuinely-measured 0.0 score ties with an unmeasured sibling instead of ranking above it) | open | |
| 2026-09-06 | high | `Flim/Views/Main/MainTabView.swift:116` (rollsPathIds) and `Flim/Views/Rolls/RollsView.swift:363,382,506,726` (NavigationLink(value: roll) pushes bypass rollsPathIds, and nothing removes from it on pop either, so rollsPathAction's dedup/truncation logic silently desyncs from the real nav stack) | fixed | e68e879 |
| 2026-09-06 | medium | `Flim/Services/RemotePush.swift:111` (claim(_:) unconditionally cancels every pending local develop-roll reminder for the account on each successful device-token registration, not just the one race it was meant to cover) | not a bug | matches owner decision in docs/PENDING.md "done 2026-09-05: the second rolls and chapters audit", commit 05933d7: broad cancellation is intended so a shooter whose first capture beat registration never gets both reminders |
| 2026-09-09 | medium | Flim/Services/FailedUploadStore.swift:104-116 (Sidecar only encodes burstGroup/sharpness, so save()/load() silently drop quality, phash, and isMiss for a capture that failed once before uploading, permanently losing its dead-frame verdict) | fixed | e68e879 |
| 2026-09-09 | medium | Flim/Services/ChapterCuration.swift:154 (qualityScore(for:) returns early on a stored quality value before requesting a Vision feature print, so any photo with stored quality scores as 0 similarity to everything in the diversity/duplicate check, defeating dedup once a month mixes pre/post-2026-09-08 photos) | open | |
| 2026-09-10 | high | FeedService.swift:974, high | fixed | 13cfba0 |
| 2026-09-10 | high | Flim/Views/Rolls/RollRevealViewModel.swift:170-182 (deck retains dead frames while playedDeck filters them out; when a roll's only surviving photos are all dead frames, playedDeck.isEmpty routes to the "shots were deleted" empty state even though frames are still visible in the roll's own grid) | fixed | e68e879 added an all-dead-frames empty state instead |
| 2026-09-10 | high | supabase/functions/send-daily-digest/index.ts:363-557 (the Deno.serve handler has no acquire_push_lock/release_push_lock around its run, unlike send-develop-push and send-social-push, so two overlapping invocations can double-send the daily digest) | fixed | e68e879 added acquire_push_lock/release_push_lock around the handler |
| 2026-09-10 | medium | Flim/Services/AuthService.swift:482-492,503-504 (setAccent has no AccountEpoch guard unlike every other write path in this file, and reconcileAccent's .uploadLocal branch fires it from an uncaptured Task, so an account switch mid-flight can write account A's accent onto account B's row) | fixed | e68e879 added an AccountEpoch guard in setAccent |
| 2026-09-10 | medium | supabase/functions/send-develop-push/index.ts:249 (acquire_push_lock lease of 240s is shorter than the 5-minute flim-develop-push cron cadence, so a run over 240s has its lock expire and the next tick starts concurrently, sending duplicate "roll developed" pushes) | fixed | 13cfba0 added a 200s RUN_BUDGET_MS that stops taking new recipients before the 240s lease can lapse |
| 2026-09-10 | medium | Flim/Views/Feed/FeedView.swift:504 and Flim/Views/Rolls/RollsView.swift:243 (FirstVisitLine marks itself seen onDisappear expecting to render for the whole first visit, but sits inside a LazyVStack, so scrolling it off and back on fires onDisappear/onAppear again and the banner vanishes mid-visit) | fixed | e68e879 replaced onDisappear with a 5s .task plus shownThisLaunch |
| 2026-09-10 | medium | Flim/Services/Activation.swift:82-109, medium | fixed | 13cfba0 |
| 2026-09-10 | low | Flim/Views/Rolls/RollDevelopAskSheet.swift:30 (timeLabel uses Calendar.isDate(_:inSameDayAs:), a midnight boundary, inconsistent with the app's 4am day boundary used elsewhere via FeedUnit.dayKey, so a roll developing between midnight and 4am is labeled with the new calendar weekday) | fixed | 05cd8bd switched timeLabel/whenLabel's same-day check to FeedUnit.dayKey |
| 2026-09-10 | low | Flim/Views/Profile/ChapterRecapViewModel.swift:194,234,237 (highlightIds from mostReacted/mostCommented/longestGap are unioned in from the unfiltered photos array rather than the dead-frame-filtered pool, so a dead-frame photo that happens to be the month's longestGap is force-included into the recap deck unlabelled) | open | |
| 2026-09-11 | medium | Flim/Services/ShareBreadcrumbs.swift:36 (occurred_at: .now is stamped inside a fire-and-forget Task after the await supabase.auth.session suspension point, so two breadcrumbs logged in true order can have occurred_at reordered depending on which task's session lookup resolves first) | fixed | 5a14004 deleted the file (the chapter share sheet is proven by a UI test now, no breadcrumbs code left to reorder) |
| 2026-09-12 | high | Flim/Services/RollService.swift:27-34 (impl), 110-111 (doc/property) (resetForAccountChange() clears rolls/memberCounts/coverPaths/error/isLoading but not followUpInvites despite its own doc comment claiming otherwise, so switching accounts on the same device leaves account A's follow-up invite card tappable for account B, and tapping Join enrolls B into A's roll) | fixed | e68e879 added followUpInvites = [] to resetForAccountChange() |
| 2026-09-12 | ? | supabase/functions/send-social-push/index.ts:272, 300-322, 729-772, 1048-1076 | fixed | 2d21be2 |
| 2026-09-12 | medium-high | Flim/Services/PhotoService.swift:1262, 1285, 1398 (uploadRenditions only retried in-process twice, 3s apart, and clearPersistedCopies deleted the local sidecar as soon as the server row was confirmed, so a phone locked/backgrounded right after the shutter could freeze a photo's renditions permanently with no retry ever surfacing it) | fixed | 0e7991f added sidecar-tracked rendition retry, deferred clearPersistedCopies, and a nightly repair sweep |
| 2026-09-14 | high | .github/workflows/nightly-numbers.yml:25-30 (rendition-repair step has no continue-on-error, so repair_renditions.py's expected exit 1 skips the Commit the line step and silently drops that night's NUMBERS.md line) | fixed | 9394d3b |
| 2026-09-14 | medium | supabase/functions/send-social-push/index.ts:1209-1230 (comment-likes push has no postVisibleTo gate like the thread-participant push does, can deep-link to a post the recipient can no longer open) | fixed | e68e879 added a postVisibleTo gate to the comment-likes push |
| 2026-09-16 | medium | supabase/migrations/2026-09-15_post_release_ops.sql:19-37 (copy_edge_failures watermarks on MAX(id) already copied, so a slow failing request completing after a faster higher-id failure was copied is silently skipped forever) | open | |
| 2026-09-17 | medium | Flim/Services/FeedSeenStore.swift:71-77 (activeUserId's didSet drops pendingSync on every account switch/sign-out with no flush first, unlike UndoCenter.shared.flush() two lines above it in ContentView, so marks made in the last few seconds before switching never reach post_seen) | fixed | d0b1129 added flushDeparting, called with the outgoing userId before pendingSync is dropped |
| 2026-09-18 | high | supabase/schema.sql:8311-8318 (BALI26 cohort insert uses a plain INSERT...VALUES with a hardcoded inviter_id instead of the guarded INSERT...SELECT...FROM public.users WHERE id=... pattern SEPT10/FLIMGO use, violating invite_campaigns.inviter_id's NOT NULL FK on a fresh database with no users rows) | fixed | 3e5680a rewrote the insert to INSERT...SELECT...FROM public.users WHERE id=... in both the migration and schema.sql |
| 2026-09-18 | low | Flim/Views/Feed/FeedView.swift:540, Flim/Views/Feed/PeopleYouKnowRow.swift:100-105 (PeopleYouKnowRow is a direct child of the same top-level LazyVStack as FirstVisitLine, already flagged 2026-09-10 for this exact mechanism, so scrolling it off-screen and back resets its @State and reruns .task, refetching loadFollowers/discoverSections) | open | |
| 2026-09-18 | low | Flim/Views/Feed/PeopleYouKnowRow.swift:101 (the row's .task gates on feed.followingIds.count < 3 at entry, but followingIds is populated by FeedView's own independent load with no ordering guarantee, so on cold launch a user who already follows 3+ people can briefly see three suggested people before the real count lands) | open | |
| 2026-09-20 | medium | Flim/Views/Rolls/RollCarouselView.swift:259,284-299 (toggleReaction's OptimisticToggle revert mutates the single un-keyed `reactions` array, and loadAround resets/refetches it on every swipe, so a failed reaction write on photo A that resolves after swiping to photo B reverts against B's reaction state instead) | fixed | 05cd8bd replaced the single reactions array with reactionsByPhoto: [UUID: [PhotoReaction]], keyed by photo id |
| 2026-09-20 | low | Flim/Models/Roll.swift:117-121 (defaultName formats the raw calendar day instead of shifting by the app's 4am day boundary like FeedUnit.dayKey, so a roll started just after midnight is prefilled with the next calendar day's name instead of the night it documents) | fixed | 05cd8bd formats FeedUnit.dayKey(for: date) instead of the raw date |
| 2026-09-22 | high | docs/sql/flim_reader_role.sql:12-15 (grants SELECT to flim_reader but never BYPASSRLS and never adds it to authenticated, so every RLS-gated table it's meant to read returns zero rows silently instead of erroring) | fixed | 039035a replaced the table-grant role with two SECURITY DEFINER functions (memo_snapshot, receiver_lookup), EXECUTE-only to flim_reader |
| 2026-09-22 | medium | Flim/Services/FeedService.swift:151-164 (fetchMyPostedPhotoIds pages posts by row position with no ORDER BY, the exact anti-pattern the file's own comment six lines above warns against for this table, so a concurrent insert during pagination can permanently skip a row) | fixed | 074dc07 switched to keyset pagination ordered and paged on posts.id |
| 2026-09-22 | low | supabase/functions/send-daily-digest/index.ts:455 (the new recentPosts read discards fetchAllPages' failed flag, unlike the client_versions read added the same commit which fails closed, so a paging error mid-run silently sends incomplete digests instead of none) | fixed | 074dc07 added the same fail-closed check on recentPostsFailed |
| 2026-09-23 | high | Flim/ContentView.swift:240 (rolls.flushPendingRevealCompletions() is called in the scenePhase-leaving-active branch, not on foreground, contradicting its own "on every foreground" doc, so an offline reveal completion is not retried until the next sign-in/restore) | open | |
| 2026-09-23 | medium | Flim/Services/WidgetSync.swift:26-52 (refresh()'s debounced Task checks Task.isCancelled only once, right after its sleep; clear()'s pending?.cancel() has no effect once run()/compose() is already executing, so a refresh started just before sign-out can write the departing account's data back after clear() ran) | open | |
| 2026-09-23 | medium | Flim/Views/Darkroom/DarkroomZoomRows.swift:322 (DarkroomAllTimeRow.init still keys shotsByMonth/topCoverByMonth with Calendar.current.component(.month, from: monthStart), the one Gregorian-parsing site tonight's four-site fix missed, so a non-Gregorian device calendar mislabels the All-time row's per-month counts and covers) | open | |
| 2026-09-25 | high | Flim/Services/FeedService.swift:1516 (restore), called unguarded from Flim/Views/Feed/FeedUnitCard.swift:713,750 and Flim/Views/Feed/PostDetailView.swift:336,364 (a failed undo commit's revert closure calls restore with no AccountEpoch guard, so a delete/block staged just before an account switch can insert the departing account's posts into the incoming account's feed once the commit fails) | open | |
| 2026-09-25 | medium | Flim/Services/RollService.swift:627-634 (deleteRoll) and Flim/Services/FeedService.swift:1524-1531 (deletePost) (neither uses .select() to detect a DELETE that RLS silently filtered to zero rows, so a request that lands under the wrong account after a mid-flight switch reports success while the row survives server-side) | open | |
| 2026-09-25 | medium | Flim/Services/RollService.swift:633,639 (deleteRoll and leaveRoll both call forget() with no AccountEpoch guard, unlike every other mutator in the file, so an account switch during the delete/leave request still ends the roll's Live Activity and rewrites the widget snapshot even when the server-side write was filtered to zero rows) | open | |
| 2026-09-25 | medium | Flim/Services/RemotePush.swift:194-218 (detachAfterSessionLoss's catch only logs and drops a failed detach_device_token call, with no persistence or retry unlike unregisterCurrentDevice's catch, so a revoked-session detach that fails on a device that never signs in again leaves that account's pushes landing on a signed-out phone indefinitely) | open | |
| 2026-09-25 | low | Flim/Views/Camera/CameraViewModel.swift:832-836 (sessionInterruptionEnded's else-branch stops the session via a detached Task with no re-check of wantsSessionRunning, so reopening the Camera tab in the narrow window before that task runs can leave the shutter live over a session the task then stops silently) | open | |
